//=========================================================
// Stage
let $stage = $('#stage');

// Google maps
let $map = $('#map');

// City canvas (rendering target of three.js)
let $cityCanvas = $('#city-canvas');

//=========================================================
export {
	$stage,
	$map,
	$cityCanvas,
};
