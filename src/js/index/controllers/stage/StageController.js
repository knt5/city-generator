import { $stage } from '../../models/stage/dom';

export default class StageController {
	resizeStage(width, height) {
		// Resize stage
		$stage.width(width);
		$stage.height(height);
		
		
		
		
		
	}
	
	
}
